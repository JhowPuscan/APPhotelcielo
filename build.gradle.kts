buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.6.0") // Asegúrate de la versión correcta de Gradle
        classpath("com.google.gms:google-services:4.4.2") // Versión consistente de Google Services
    }
}
